import React from "react";
import { Typography, Box } from "@mui/material";

const Products: React.FC = () => (
  <Box>
    <Typography variant="h4">Products</Typography>
    <Typography>Product list coming soon.</Typography>
  </Box>
);

export default Products;
