import React from "react";
import { Typography, Box } from "@mui/material";

const Orders: React.FC = () => (
  <Box>
    <Typography variant="h4">Orders</Typography>
    <Typography>Order list coming soon.</Typography>
  </Box>
);

export default Orders;
